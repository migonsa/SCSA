Previous requirements:
	- Having python 3.8 and "venv"
	- Having installed python in the path


Installing notes:

1º Open windows CMD
2º Type "create_env" to create an environment called "scsa-env" and activating it.
3º Type "install" to install all necessary python packages.
4º Type "run" to run the python application SCSA.
